\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{e_colors}[2025/06/20 Custom color definitions from e_style]

% Require the xcolor package that your color definitions rely on
\RequirePackage[table,x11names,dvipsnames]{xcolor}

% Input the color definitions. This path works because
% c120_color.tex is in a subdirectory relative to this new file.
\input{../sty_components/c120_color.tex}

\endinput