\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{e_core}[2026/07/11 v2.0 DEPRECATED compatibility wrapper for e_profile_common]

% [DEPRECATED / SUPERSEDED-BY: e_profile_common]
% Backward compatibility for existing documents. New code should select an
% explicit profile instead of treating the historical full bundle as a kernel.
\RequirePackage{e_profile_common}

\endinput
