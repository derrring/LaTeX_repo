\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{e_standard_doc}[2025/06/20 Style for standard document classes]

% --- 1. Load the Core Library ---
% This gives us access to all the common colors, math, commands, etc.
\RequirePackage{e_core}

% --- 2. Load Packages that CONFLICT with Beamer ---
% Hyperlinks
\RequirePackage[unicode]{hyperref}
\RequirePackage{bookmark}
\RequirePackage{geometry}
%control headers and footers
\RequirePackage{fancyhdr}

\RequirePackage{titlesec,titletoc,etoc} % titlesec is the main conflicting package

% List items
\RequirePackage{enumitem}

% --- 3. Apply Settings that CONFLICT with Beamer ---

\input{sty_components/c110_layout.tex}
\input{sty_components/c130_toc.tex}
\input{sty_components/c320_math_env.tex}

\hypersetup{
    colorlinks=true,
    linkcolor=mylinkcolor,
    citecolor=mycitecolor,
    urlcolor=mylinkcolor,
    anchorcolor=mylinkcolor,
    filecolor=mylinkcolor,
    linktoc=page,
    hypertexnames=false,  % Fix duplicate destinations for section-numbered counters
}
\makeatletter
\renewcommand{\theHtheorem}{\thetheorem}
\renewcommand{\theHdefinition}{\thedefinition}
\renewcommand{\theHlemma}{\thelemma}
\renewcommand{\theHproposition}{\theproposition}
\renewcommand{\theHcorollary}{\thecorollary}
\makeatother
\endinput